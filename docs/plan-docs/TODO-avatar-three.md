# 🔴 미정리 — three.js 아바타 실험

**날짜:** 2026-08-27 · **상태:** 실험만 붙였고 **문서는 아직 안 고쳤다**

되는지 먼저 보기로 했다. 유지하기로 하면 아래를 고쳐야 한다. **버리기로 하면 되돌리면 된다.**

## 고쳐야 할 문서

| 무엇 | 지금 | 해야 할 것 |
| --- | --- | --- |
| `ADR-T05` | 아바타를 Lottie 2.5D로 | 3D 유지로 되돌림 · 「회전이 사라진다」 대가 삭제 |
| `X2` (충돌 대장 §8) | 해소 | **재개** — 3D 렌더러를 넣기로 했으므로 |
| `REQ-TEC-007` | UI 의존성 0건 · Lottie 예외 1건 | 예외를 `three` 로 교체 또는 2건으로 |
| `STR-003` 제약 | *"다른 렌더러를 추가하지 않는다"* | 문구 수정 (`tools/tasks_data.py` 를 고치고 재생성) |
| `prebuild` 게이트 3번 | 허용 목록 밖 컴포넌트 킷 차단 | `three` 등록 — 안 하면 **빌드 실패** |
| 명세 §4.2.1 | CSS 3D 로 세운 이유 | three.js 채택 시 다시 씀 |
| 원장 T15 | 이모지 + CSS 3D | 결정 갱신 · 새 토픽으로 |

## 아직 열려 있는 것

- **D4** — 3D 아바타 사양(동물 종수 · 의상 벌수 · 가격표). **팀 결정 전**
- **B4** — 3D 에셋 제작비. 🔴 D4 확정 전 제작 착수 금지 · **선불이라 사후 조정 불가**
- 지금은 에셋 없이 **원시 도형**으로 세웠다. 실제 캐릭터는 D4·B4를 지나야 한다

## 실측값 (2026-08-27)

| 항목 | 값 |
| --- | --- |
| `three` 버전 | 0.185.1 |
| three 청크 | gzip **155 KB** (환경맵·GLTFLoader 포함) |
| 캐릭터 GLB | **246 KB** + 텍스처 9 KB |
| 기본(이모지) 화면이 청크를 부르는가 | **아니다** — `dynamic(ssr:false)` 라 `?avatar=` 가 있을 때만 |
| WebGL 없는 기기 | 폴백 확인 — 이모지 + *"이 기기에서는 3D를 못 그려요"* |

증거: `reports/proto/avatar-3ways.png` · `avatar-socket.png` · `avatar-three-angles.png`

## 표현 3종 — 비교 (`?avatar=` 로 전환)

| 모드 | 무엇 | 자산 |
| --- | --- | --- |
| `css` (기본) | 이모지 + CSS 3D 회전 | 0 |
| `?avatar=three` | 원시 도형 + 환경맵·톤매핑·접지 그림자 | three 만 |
| `?avatar=glb` | Kenney CC0 모델 + `idle` 애니메이션 | three + 246 KB |

**렌더링만 고쳐도 체감이 크게 달라졌다.** 환경맵(`RoomEnvironment` → PMREM)이 제일 컸고,
톤매핑·접지 그림자가 뒤를 받쳤다. 에셋 없이 되는 작업이다.

## 소켓 규약 — 실제로 해보고 알게 된 것

| | |
| --- | --- |
| 이름 | `socket_head` · `socket_back` · `socket_neck`. 없으면 `head`·`torso`·`spine` 등으로 대체 탐색 |
| **높이를 숫자로 박으면 안 된다** | Kenney 는 `head` 뼈가 **목 근처**에 있다. 다른 팩은 정수리에 있다. 숫자를 박으면 모델을 바꾸는 순간 모자가 머리에 박힌다 |
| 치수는 **캐릭터 전체 바운딩 박스**로 잰다 | 뼈·빈 노드는 메시가 없어 박스가 **비어 있다.** 소켓 박스로 재면 아이템이 0 배율이 돼 사라진다 |
| 부모는 소켓, 치수는 루트 | 애니메이션을 따라가려면 부모는 뼈여야 한다 |
| 소켓의 회전·스케일을 되돌린다 | 안 하면 아이템이 뒤틀린다 |

🔴 **남은 한계** — 바운딩 박스 꼭대기는 **머리 꼭대기가 아니다.** 곰은 귀가, 사람은 머리카락이
더 높다. 그래서 모자가 살짝 뜬다. **`D4` 사양에 「소켓 이름」뿐 아니라 「아이템별 오프셋」도
넣어야 한다.** 규약이 90%를 해결하고 나머지 10%는 에셋 사양이 들고 있어야 한다.

## 아직 안 한 최적화

- `gltf-transform` 으로 **텍스처 임베드 + Draco/Meshopt 압축 + KTX2** — 지금은 텍스처가
  `public/models/Textures/colormap.png` 로 **분리돼 있다.** 받은 GLB 가 외부 URI 를 참조한다
- 캐릭터 GLB 를 화면에 하나만 쓰는데 **애니메이션 32종이 다 들어 있다.** 쓰는 것만 남기면 준다

---

## 2026-08-31 — 아바타·펫·아이템으로 확장

방향이 바뀌었다. **아바타는 사람, 동물은 펫**이다.

| | |
| --- | --- |
| 아바타 | Kenney Mini Characters (사람) 고정 |
| 펫 | Kenney **Cube Pets** — 동물 24종 중 6종 · CC0 |
| 아이템 | kit 하나가 카테고리 하나 — 가구 · 화분 · 먹을 것 · 탈것 |
| 화면 | `/child/home` 방 3D · `/child/shop` 별로 바꾸기 (라우트 14건) |

### 아이템은 두 갈래다

| 종류 | 붙는 곳 | 구현 |
| --- | --- | --- |
| 착용 | 아바타 **소켓** | `attachToSocket` — 이미 있던 것 |
| 배치 | 방 **바닥 좌표** | `loadProp` + 카탈로그의 `x·z·ry` |
| 펫 | 아바타 **옆** | 고정 좌표 |

카탈로그(`app/src/app/child/home/room.fixture.ts`)가 어느 쪽인지 들고 있고 화면은 따르기만 한다.

### 걸렸던 것

- **kit 마다 텍스처가 다르다.** 각 GLB 가 `Textures/colormap.png` 를 상대 경로로 참조한다.
  카테고리 폴더마다 그 kit 의 colormap 을 넣어야 한다. 하나로 합치면 색이 섞인다
- **원본 스케일이 kit 마다 다르다.** 카탈로그의 `size`(최대 변 길이)로 통일하고
  `loadProp` 이 바닥에 앉힌다. 안 하면 침대가 아바타보다 커진다

### 🔴 문서에 아직 안 반영한 것

이번 확장은 **`STR-005`(별의 옷장 외 목적지)** 그 자체다. 그 태스크의 착수 조건이
**「현금 분리선 재검토 통과 확인 — 미통과 시 착수 불가」** 인데 **재검토를 하지 않았다.**

방향은 `P-21` 안쪽이다 — 앱 내 재화일 뿐 현물·현금·제휴처가 아니다. 화면에도 적어 뒀다
(*"별은 방 아이템으로만 바꿀 수 있어요. 돈으로 바꾸지는 않아요"*). 다만 **절차를 건너뛴 것은 사실**이다.

`D4` 도 함께 바뀐다 — 「동물 종수 · 의상 벌수」에서 **「펫 종수 · 카테고리별 아이템 수 · 가격표」** 로.
CC0 라 `B4`(선불 제작비)가 거의 0 이 되고 리스크 `R9` 이 사라진다.

### 남은 설계 질문

**가격표가 학습 방향을 정한다.** 상점이 커지면 즉시 소비를 부추겨 「모으기」와 부딪힌다.
위시리스트(`PRC-004`)가 이미 모으기를 가르치는 장치라 서로 경쟁한다.
지금은 상점에 *"지금 안 쓰고 모으면 더 큰 걸 바꿀 수 있어요"* 한 줄로 눌러 뒀지만,
**종수·벌수보다 가격표가 더 중요한 `D4` 결정일 수 있다.**

---

## 2026-08-31 (2) — 아이템 확장 · 방 꾸미기

### 아이템 55개

| 카테고리 | 개수 | 원본에 남은 것 |
| --- | :-: | --- |
| 옷장(착용) | 2 | 도형이라 원본 없음 |
| 펫 | 11 | Cube Pets 24종 중 |
| 가구 | 16 | Furniture Kit 140종 중 |
| 화분 | 10 | Nature Kit 329종 중 |
| 먹을 것 | 10 | Food Kit 200종 중 |
| 탈것 | 6 | Toy Car Kit 157종 중 |

**더 넣는 건 카탈로그에 줄 하나 추가하는 일이다.** 총 3.2MB.

### 방 꾸미기 모드

방 아래 **「🛠 방 꾸미기」** 버튼으로 들어간다. 보기 모드와 조작이 다르다.

| | 보기 | 꾸미기 |
| --- | --- | --- |
| 끌기 | 방이 돈다 | **아이템이 따라온다** |
| 누르기 | — | 아이템을 고른다 (바닥에 초록 링) |
| 버튼 | 「방 꾸미기」 | 왼쪽·오른쪽 회전 · 처음으로 · 다 꾸몄어요 |

- 아이템을 고르면 문구가 바뀐다 — *"「고양이」를 골랐어요 · 끌어서 옮기기"*
- 바닥 반지름 4.4 밖으로는 못 나간다
- 배치는 `localStorage` 에 남는다. 🔴 **프로토타입 전용** — 본 개발에서는 서버가 갖는다
- 아무것도 안 고르고 끌면 방이 돈다. 아이가 실수로 아이템을 못 옮기게

### 🔴 검증 방법이 하나 늘었다 — `tools/verify_room_drag.py`

**스크린샷으로는 「끌어서 옮기기」가 되는지 알 수 없다.** 헤드리스는 클릭을 못 한다.
그래서 크롬을 디버깅 포트로 띄우고 **CDP `Input.dispatchMouseEvent` 로 진짜 드래그**를 넣는다.

판정은 두 가지가 다 성립해야 통과다 — **아이템이 선택되고**, **`localStorage` 배치값이 바뀐다.**
실제로 고양이를 `(1.25, 1.35)` → `(-1.43, -1.2)` 로 옮기고 저장되는 것을 확인했다.
